#let paramAttibut =  ("params", [params Objekt @pass-data-schema], "Optional", "Informationen zur Kfz-Versicherung nach FRIDA-Standard")

#let expiredAttribut = ("expired", "boolean", "Optional", "Der Ablaufstatus. Der Wert ist true, wenn Versicherungsvertrag abgelaufen ist. Der QR-Code des Passes wird daraufhin ausgegraut und die persönlichen Information gelöscht.")


#let xApiKeyAttribut = ("X-API-Key", "string", "Pflicht", "Der API-Key für die Authentifizierung", "JDI241WA4242")